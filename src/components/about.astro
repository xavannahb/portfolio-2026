<section id="about">
    <div class="eyebrow mono">
        <span><span class="index">01</span> — Over mij</span>
        <span>Amsterdam, NL</span>
    </div>
    
    <div class="content">
        <h1 class="heading">Over mij</h1>
    
        <div class="copy">
            <p class="lede">Ik ontwerp interfaces die niet alleen functioneel zijn, maar ook gebruiksvriendelijk en aantrekkelijk.</p>
            <p class="body">Right now that means sketching flows, testing them on real people, and turning the result into clean, animated front-end. I'm happiest somewhere between Figma and a code editor, and I'm looking for a team that will let me keep one foot in each.</p>
        </div>
    
        <div class="skills mono">
        <div class="group">
            <h2>Design</h2>
            <ul>
                <li>UX Research</li>
                <li>Wireframing</li>
                <li>Interaction</li>
            </ul>
        </div>
        <div class="group">
            <h2>Bouw</h2>
            <ul>
                <li>HTML / CSS</li>
                <li>JavaScript</li>
            </ul>
        </div>
        <div class="group">
            <h2>Tools</h2>
            <ul>
                <li>Figma</li>
                <li>VS Code</li>
                <li>GitHub</li>
            </ul>
        </div>
        </div>
    </div>
</section>
<style>

.mono {
    font-family: "Courier New", Courier, monospace;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}
 
section {
    max-width: 1800px;
    margin: 0 auto;
    padding: 0 40px;
    border-top: 1px solid var(--line);
}
 
.eyebrow {
    display: flex;
    justify-content: space-between;
    padding: 22px 0;
    font-size: 12px;
}
 
.eyebrow .index { 
    color: var(--red); 
}
 
.content {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 60px;
    padding-bottom: 60px;
}
 
h1.heading {
    margin: 40px 0 0;
    font-size: clamp(56px, 6.5vw, 90px);
    font-weight: 800;
    line-height: 0.92;
    color: var(--red);
    letter-spacing: -0.01em;
}
 
.copy {
    padding-top: 40px;
}
 
.copy .lede {
    font-size: 26px;
    line-height: 1.4;
    margin: 0 0 32px;
    max-width: 34ch;
}
 
.copy .body {
    font-size: 17px;
    line-height: 1.7;
    color: var(--text);
    max-width: 42ch;
    margin: 0;
}
 
.skills {
    grid-column: 2 / 3;
    border-top: 1px solid var(--line);
    padding-top: 24px;
    display: flex;
    gap: 90px;
}
 
.skills .group h2 {
    font-size: 12px;
    font-weight: 400;
    color: var(--red);
    margin: 0 0 14px;
}
 
.skills .group ul {
    list-style: none;
    margin: 0;
    padding: 0;
}
 
.skills .group li {
    font-size: 13px;
    line-height: 2.1;
}
 
@media (max-width: 900px) {
    .content {
        grid-template-columns: 1fr;
        gap: 0;
    }
    .copy {
        padding-top: 24px;
        grid-column: 1 / 2;
    }
    .skills {
        grid-column: 1 / 2;
        flex-wrap: wrap;
        gap: 40px;
    }
}
</style>
