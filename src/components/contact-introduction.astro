<section>
    <!-- Squigles -->
    <div class="loop">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 120" width="200" height="120" fill="none">
            <path d="M5,40 C20,5 50,5 60,30 C70,55 35,60 30,35 C25,15 70,15 85,40 C100,65 65,75 60,50 C55,25 110,25 125,55 C140,80 100,90 95,65 C90,40 150,40 165,70 C180,100 130,110 125,80"
            stroke="#1f3fc7"
            stroke-width="2.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            style="stroke-dasharray: 564; stroke-dashoffset: 564; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>
    <div class="star">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 60 60" width="60" height="60" fill="none">
            <path d="M30,5 L30,55 M5,30 L55,30 M12,12 L48,48 M48,12 L12,48"
                stroke="#1f3fc7"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                style="stroke-dasharray: 202; stroke-dashoffset: 202; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>

    <!-- Content -->
    <p>Contact</p>
    <h1>Say hi —<span class="lastname">I read everything.</span></h1>
    <p>Best on email. I reply within a day or two — usually with too many questions about your project, your team, and whether you have a strong opinion on cortado vs flat white.</p>
    
</section>
<style>
    section {
        padding: clamp(40px, 6vw, 80px) 0px 60px;
        position: relative;
    }

    /* Squigles positioneren */

    div.loop {
        position: absolute;
        top: 90px;
        right: 8%;
        pointer-events: none;
    }
    
    div.star {
        position: absolute;
        top: calc(60% + 20px);
        
        pointer-events: none;
    }

    @keyframes draw-path {
        to { stroke-dashoffset: 0; }
    }

    p:nth-of-type(1) {
        font-size: 12px;
        letter-spacing: 0.22em;
        text-transform: uppercase;
        color: rgb(90, 85, 76);
        position: relative;
        display: inline-block;
    }
    
    h1 {
        font-family: Caveat, cursive;
        font-size: clamp(56px, 9vw, 132px);
        font-weight: 600;
        line-height: 0.9;
        letter-spacing: -0.01em;
        margin: 20px 0px 0px;

    }

    span.lastname {
        display: inline-block;
        margin-left: 12%;
    }

    p {
        margin-top: 32px;
        max-width: 540px;
        font-size: 15px;
        line-height: 1.6;
        color: var(--text-color-grey);
    }
</style>
