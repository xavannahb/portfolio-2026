<section>
    <!-- Squigles -->
    <div class="line">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 60" width="300" height="60" fill="none">
            <path d="M5,30 C40,10 70,55 110,30 C150,5 180,55 220,25 C240,10 260,35 290,20"
            stroke="#1f3fc7"
            stroke-width="2.2"
            stroke-linecap="round"
            stroke-linejoin="round"
            style="stroke-dasharray: 426; stroke-dashoffset: 426; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>
    <div class="loop">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 120" width="200" height="120" fill="none">
            <path d="M5,40 C20,5 50,5 60,30 C70,55 35,60 30,35 C25,15 70,15 85,40 C100,65 65,75 60,50 C55,25 110,25 125,55 C140,80 100,90 95,65 C90,40 150,40 165,70 C180,100 130,110 125,80"
            stroke="#1f3fc7"
            stroke-width="2.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            style="stroke-dasharray: 564; stroke-dashoffset: 564; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>
    <div class="star">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 60 60" width="60" height="60" fill="none">
            <path d="M30,5 L30,55 M5,30 L55,30 M12,12 L48,48 M48,12 L12,48"
                stroke="#1f3fc7"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                style="stroke-dasharray: 202; stroke-dashoffset: 202; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>

    <!-- Content -->
    <p>Portfolio · 2024 – 2026 · selected works</p>
    <h1>Xavannah <span class="lastname">Baars</span>,</h1>
    <p>a <span class="frontend">frontend designer</span> based in Amsterdam — building soft, considered interfaces & the occasional internet curiosity.</p>
    
    <div class="buttons">
        <a href="/work">See my work<span style="margin-left: 10px; display: inline-block; transform: translateY(1px);">↘</span></a>
        <a href="/about">Learn about me</a>
    </div>
</section>
<style>
    section {
        padding: clamp(40px, 8vw, 100px) 0px clamp(80px, 12vw, 140px);
        text-align: center;
    }

    /* Squigles positioneren */
    div.line {
        position: absolute;
        top: 20px;
        left: 8%;
        pointer-events: none;
    }

    div.loop {
        position: absolute;
        top: 90px;
        right: 8%;
        pointer-events: none;
    }
    
    div.star {
        position: absolute;
        top: calc(20% + 20px);
        left: 14%;
        pointer-events: none;
    }

    @keyframes draw-path {
        to { stroke-dashoffset: 0; }
    }

    p:nth-of-type(1) {
        font-size: 11px;
        letter-spacing: 0.28em;
        text-transform: uppercase;
        color: var(--text-color-grey);
        margin-bottom: 32px;
        animation: 0.6s ease 0.2s 1 normal both running h-fade;
    }
    
    h1 {
        font-family: Caveat, cursive;
        font-size: clamp(96px, 14vw, 196px);
        font-weight: 600;
        line-height: 0.9;
        letter-spacing: -0.01em;
        margin: 0px;
    }

    span.lastname {
        display: inline-block;
        margin-left: 12%;
    }

    span.frontend {
        display: inline-block;
        font-family: Caveat, cursive;
        font-size: 28px;
        padding: 0rem 0.5rem;
        color: var(--accent-color);

    }

    p {
        max-width: 460px;
        margin: 40px auto 0px;
        font-size: 16px;
        line-height: 1.55;
        letter-spacing: 0.01em;
        color: var(--text-color);
        opacity: 0.88;
        animation: 0.8s ease 1.1s 1 normal both running h-fade;
    }
    
    div.buttons {
        margin-top: 48px;
        display: flex;
        justify-content: center;
        gap: 28px;
    }

    div.buttons a:nth-of-type(1) {
        display: inline-flex;
        align-items: center;
        background: var(--accent-color);
        color: rgb(243, 237, 225);
        text-decoration: none;
        padding: 14px 22px;
        border-radius: 999px;
        text-transform: uppercase;
    }

    div.buttons a:nth-of-type(2) {
        display: inline-flex;
        align-items: center;
        color: var(--text-color);
        text-decoration: none;
        padding: 14px 6px;
        font-size: 13px;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        border-bottom: 1px solid var(--text-color);
    }
</style>