<section>
    <!-- Squigles -->
    <div class="line">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 60" width="300" height="60" fill="none">
            <path d="M5,30 C40,10 70,55 110,30 C150,5 180,55 220,25 C240,10 260,35 290,20"
            stroke="#1f3fc7"
            stroke-width="2.2"
            stroke-linecap="round"
            stroke-linejoin="round"
            style="stroke-dasharray: 426; stroke-dashoffset: 426; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>
    <div class="star">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 60 60" width="60" height="60" fill="none">
            <path d="M30,5 L30,55 M5,30 L55,30 M12,12 L48,48 M48,12 L12,48"
                stroke="#1f3fc7"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                style="stroke-dasharray: 202; stroke-dashoffset: 202; animation: draw-path 1.2s ease-in-out forwards;"/>
        </svg>
    </div>

    <div class="img-grid">
        <div class="img-one">
            <img src="/images/nebula.png" alt="Project 1">
            <p>02</p> 
        </div>
        <div class="items two"></div>
        <div class="items three"></div>
        <div class="items four"></div>
        <div class="items five"></div>
        <div class="items six"></div>
    </div>

</section>
<style>
    section {
        padding: clamp(40px, 8vw, 100px) 0px clamp(80px, 12vw, 140px);
        text-align: center;
    }

    /* Squigles positioneren */
    div.line {
        position: absolute;
        top: 73%;
        right: 20%;
        pointer-events: none;
    }

    div.star {
        position: absolute;
        top: 90%;
        right: 14%;
        pointer-events: none;
    }

    /* Grid styling */
    .img-grid {
        position: relative;
        display: grid;
        grid-template-columns: repeat(1, 1fr);
        gap: clamp(16px, 3vw, 48px);
    }


    img {
        width: 100%;
        height: auto;
        display: block;
        border-radius: 5px;
        box-shadow: rgba(0, 0, 0, 0.05) 0px 1px 0px, rgba(0, 0, 0, 0.4) 0px 18px 36px -22px;
    }

    p {
        display: inline-block;
        font-family: Caveat, cursive;
        font-size: 24px;
        padding: 0rem 0.5rem;
        color: var(--accent-color);
    }

    .img-one:hover {
        transform: translateY(-4px);
        transition: transform 0.3s ease;  
    }


    @media (width > 800px) {
        .img-grid {
            grid-template-columns: repeat(11, 1fr);
        }

        .img-one {
            grid-column: span 5;
        }
    }
</style>