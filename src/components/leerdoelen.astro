<section>
    <div class="squigles-container">
        <!-- Squigles -->
        <div class="line">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 60" width="300" height="60" fill="none">
                <path d="M5,30 C40,10 70,55 110,30 C150,5 180,55 220,25 C240,10 260,35 290,20"
                stroke="#1f3fc7"
                stroke-width="2.2"
                stroke-linecap="round"
                stroke-linejoin="round"
                style="stroke-dasharray: 426; stroke-dashoffset: 426; animation: draw-path 1.2s ease-in-out forwards;"/>
            </svg>
        </div>
        <div class="star">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 60 60" width="60" height="60" fill="none">
                <path d="M30,5 L30,55 M5,30 L55,30 M12,12 L48,48 M48,12 L12,48"
                    stroke="#1f3fc7"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    style="stroke-dasharray: 202; stroke-dashoffset: 202; animation: draw-path 1.2s ease-in-out forwards;"/>
            </svg>
        </div>
    </div>

    <!-- Content -->
    <div class="leerdoelen">
        <article class="card one">
            <div class="card-top">
                <span class="card-num">leerdoel 01</span>
                <span class="card-tag">Persoonlijk</span>
            </div>
            <h3>Gebruikerservaring verbeteren</h3>
            <p>Ik wil mijn kennis van CSS-animaties uitbreiden zodat ik vloeiende en gebruiksvriendelijke animaties 
            kan toepassen die de gebruikerservaring verbeteren.</p>
        </article>

        <article class="card two">
            <div class="card-top">
                <span class="card-num">leerdoel 02</span>
                <span class="card-tag">Persoonlijk</span>
            </div>
            <h3>Mobile-first ontwerpen</h3>
            <p>Ik wil leren hoe ik mobiele ontwerpen zo gebruiksvriendelijk mogelijk kan maken door altijd mobile-first
            te ontwerpen en vanaf daar het ontwerp uit te breiden.</p>
        </article>

        <article class="card three">
            <div class="card-top">
                <span class="card-num">leerdoel 03</span>
                <span class="card-tag">Persoonlijk</span>
            </div>
            <h3>JavaScript</h3>
            <p>Ik wil mijn JavaScript-vaardigheden verdiepen door complexere functionaliteiten te bouwen in websites. 
            Ik wil graag interactieve elementen kunnen ontwerpen en beter worden met API's.</p>
        </article>

        <article class="card four">
            <div class="card-top">
                <span class="card-num">leerdoel 04</span>
                <span class="card-tag">Professioneel</span>
            </div>
            <h3>Gestructureerd werken</h3>
            <p>Ik wil leren om gestructureerd werken door de werkzaamheden beter te organiseren, realistische planningen
            op te stellen en efficiënt om te gaan met tijd en prioriteiten tijdens projecten.</p>
        </article>
    </div>

</section>
<style>
    section {
        padding: clamp(40px, 8vw, 80px) 0px;
        text-align: center;
    }

    /* Squigles positioneren */
    .squigles-container {
        position: relative;
    }

    div.line {
        position: absolute;
        top: clamp(150px, 30vw, 250px);
        right: 20%;
        width: clamp(150px, 20vw, 300px); /* pas aan op basis van de viewBox-breedte van deze SVG */
        pointer-events: none;
    }

    div.star {
        position: absolute;
        left: 10px;
        top: clamp(300px, 55vw, 550px);
        width: clamp(35px, 5vw, 60px); /* pas aan op basis van de viewBox-breedte van deze SVG */
        pointer-events: none;
    }

    .line svg,
    .star svg {
        width: 100%;
        height: auto;
        display: block;
    }

    /* Porjects grid */
    .leerdoelen {
        position: relative;
        display: grid;
        grid-template-columns: repeat(1, 1fr);
        gap: clamp(30px, 3vw, 60px);
    }

    .card {
        background: #fffaf3;
        border-radius: 12px;
        padding: 1.75rem;
        display: flex;
        flex-direction: column;
        gap: 0.85rem;
        transition: box-shadow var(--ease), transform var(--ease);
        box-shadow: 0 2px 8px rgba(46, 64, 87, 0.05);
    }

    .card-top {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }   

    .card-tag {
        font-weight: 700;
        font-size: 12px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        border: 0.5px solid currentColor;
        border-radius: 999px;
        padding: 4px 12px;
        color: var(--text-color-grey);
    }

    h3 {
        color: var(--accent-color);
        font-family: 'Caveat', cursive;
        font-size: 1.8rem;
        font-weight: 400;
        margin: 0;
    }

    p {
        font-size: var(--text-small);
        color: var(--text-color-grey);
    }

    
    /* Hover states for each project */
    .one {
        transform: rotate(1deg);
    }

    .three {
        transform: rotate(-2deg);
    }

    .one:hover {
        transform: rotate(0deg);
        transition: transform 0.3s ease;
    
    }

    .two:hover {
        transform: rotate(2deg);
        transition: transform 0.3s ease;
    }

    .three:hover {
        transform: rotate(0deg);
        transition: transform 0.3s ease;
    }

    .four:hover {
        transform: rotate(-1deg);
        transition: transform 0.3s ease;
    }

    @media (width > 800px) {
        .leerdoelen {
            grid-template-columns: repeat(11, 1fr);
        }

        .one {
            grid-column:  2 / 7;
            translate: 20px 0px;
        }

        .two {
            grid-column:  8 / 12;
        }

        .three {
            grid-column:  1 / 6;
        }

        .four {
            grid-column:  6 / 11;
        }

    }
</style>